import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.examplle.irrigation',
  appName: 'smart_irrigation',
  webDir: 'dist'
};

export default config;
